# MCP Integration Requirements for sanmcp
# Install with: pip install -r requirements.txt

# Core MCP dependencies
fastmcp>=2.8.0          # FastMCP framework used by bayer-int/mcp-servers
certifi                 # SSL certificates for HTTPS connections

# Configuration and data handling  
pyyaml>=6.0             # YAML configuration file support
python-dotenv>=1.0      # Environment variable loading

# Optional: AWS integration (if using Secrets Manager or Lambda)
boto3>=1.26.0           # AWS SDK for Python
aws-lambda-powertools>=2.0.0  # AWS Lambda utilities (if deploying to Lambda)

# Optional: Data science and ML libraries
pandas>=1.5.0           # Data manipulation (if processing query results)
numpy>=1.21.0           # Numerical computing

# Development and testing dependencies  
pytest>=7.0.0           # Testing framework
pytest-asyncio>=0.21.0  # Async testing support

# Optional: Jupyter notebook support (for data exploration)
jupyter>=1.0.0          # Jupyter notebook
ipython>=8.0.0          # Enhanced Python REPL

# Optional: Data visualization
matplotlib>=3.5.0       # Plotting library
seaborn>=0.11.0         # Statistical data visualization

# Optional: Advanced ML libraries
scikit-learn>=1.1.0     # Machine learning library
joblib>=1.1.0           # Parallel computing

# Optional: Database-specific drivers (if needed for direct connections)

pg8000>=1.29.0          # Pure Python PostgreSQL driver (used by MCP server)



# Optional: Production monitoring and logging
structlog>=22.0.0       # Structured logging
sentry-sdk>=1.0.0       # Error tracking and performance monitoring
